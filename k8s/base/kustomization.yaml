apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: inference-gateway

resources:
  - namespace.yaml
  - configmap.yaml
  - redis.yaml
  - gateway.yaml
  - celery-worker.yaml
  - ingress.yaml